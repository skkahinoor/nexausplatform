import { Layout } from '@/components/Layout';
import { WorkSection } from '@/components/WorkSection';

const Work = () => {
  return (
    <Layout>
      <div className="pt-24">
        <WorkSection />
      </div>
    </Layout>
  );
};

export default Work;

